{
  "findings": [
    {
      "confidence": "high",
      "file": "service/src/test/java/com/example/urlshortener/behaviour/HostEvasionRefusalTest.java",
      "id": "TEST-1",
      "line": 45,
      "severity": "major",
      "suggestion": "On a scratch worktree, revert only implement's three normalisation hunks \u2014 HostNormalizer's body, UrlValidator.requireShortenable (line 129-131), DenylistThreatCheck.evaluate (line 55-64) \u2014 and run HostEvasionRefusalTest and HostCanonicalisationTest against the current test tree. Attach the failing output (expect aDenylistedHostWithATrailingDotIsRefusedIdenticallyToTheHostItself and anInternalAddressWrittenAsOneDecimalNumberIsRefusedIdenticallyToTheDottedForm red) to the run record as AC3's evidence, or have a human waive the ordering constraint explicitly.",
      "summary": "AC3 has two halves and only one is evidenced. The tests exist and are retained, but \"fail against the pre-change code\" was never observed: the run journal shows `author-tests` and `implement` started in parallel (seq 71-72), author-tests' exit gates were tests_compile / tests_not_weakened / paths_confined with no execution, and the first suite run was `maven_verify` at the `verify` node, after the join \u2014 i.e. only ever against the fixed code. plan.json's T1 names \"run log entry recording the failing run against pre-change code\" as a deliverable and there is no such entry; test-contract.json's own rationale flagged this as \"a pipeline obligation... the single easiest thing here to lose\". By inspection the test does discriminate (pre-change, requireShortenable lower-cased the raw host and DenylistThreatCheck matched the raw host, so https://malware.example.com./x returned 201), but \"by inspection\" is exactly what AC3 refuses. Release-readiness must not mark AC3 met on the strength of a green post-fix suite."
    },
    {
      "confidence": "medium",
      "file": "service/src/test/java/com/example/urlshortener/behaviour/AnonymousCreateDegradationTest.java",
      "id": "TEST-2",
      "line": 87,
      "severity": "minor",
      "suggestion": "Add a behaviour whose only outcome is the refusal \u2014 e.g. anonymousCreateIsRefusedWith503WhenTheCacheCannotBeInvalidated \u2014 asserting exactly 503, error=service_unavailable and message \"Temporarily unable to accept new links.\" with the counter tier paused, rather than tolerating the success. If a 201 genuinely remains permissible for the shared tier, drive the outage at the dependency the pre-issue invalidation actually needs so one answer is determined.",
      "summary": "The behaviour named for AC15 cannot fail on the thing AC15 asks for. anonymousCreationIsRefusedWithTheExistingErrorWhileADependencyIsUnavailable accepts `201 || 503`, and its three following assertions on the service_unavailable envelope are filtered on `statusCode() != 201` / `== 503`, so they are all vacuous when the answer is 201. Delete `resolutionCache.invalidateBeforeIssuing(...)` from LinkService.createAnonymous \u2014 the one call that produces the 503 there \u2014 and this test still passes. The guarantee survives only through bothCreatePathsDegradeTheSameWay, which compares the two paths' statuses; that single equality is the whole pin, and the same disjunctive tolerance on the authenticated path was already recorded as a finding in the previous run's review-test-adequacy.json."
    },
    {
      "confidence": "high",
      "file": "service/src/main/java/com/example/urlshortener/link/UrlValidator.java",
      "id": "TEST-3",
      "line": 130,
      "severity": "minor",
      "suggestion": "A unit test in com.example.urlshortener.unit \u2014 UrlValidator is deliberately constructible without Spring (LinkPolicyConfig's javadoc says so) \u2014 e.g. ownHostIsRefusedInEverySpelling: `new UrlValidator(\"short.example\")` and assert url_rejected for https://short.example./x and https://SHORT.Example/x, and no exception for https://notshort.example/x. Optionally the HTTP counterpart with app.base-url=https://short.example overridden on its own context.",
      "summary": "New behaviour with no test that can see it: the self-referential check now compares canonical hosts (canonicalOwnHost, computed once in the constructor) so that a trailing-dot or numeric spelling of the shortener's own host is refused. No test distinguishes it. app.base-url is http://localhost:8080 in every context the suite boots, so ownHost is \"localhost\", and isInternal(\"localhost\") returns true regardless \u2014 every spelling of the own host is refused by the internal-address rule whether or not canonicalisation happens. Revert line 130 to the previous `host.equals(ownHost.toLowerCase(Locale.ROOT))` and the suite stays green; Fixtures.SELF_REFERENTIAL_URL (http://localhost:8080/somewhere) does not separate the two rules either."
    },
    {
      "confidence": "medium",
      "file": "service/src/test/java/com/example/urlshortener/behaviour/AnonymousLinkExpiryTest.java",
      "id": "TEST-4",
      "line": 93,
      "severity": "minor",
      "suggestion": "Give this class a longer override (e.g. app.links.anonymous-ttl=PT10S) so the mid-life click has seconds of margin rather than a fraction of one, or assert the 302 only after re-reading the clock on the response and failing with a message that names the elapsed time. Do not relax the 302 expectation itself.",
      "summary": "Timing slack too thin for the click path this suite runs. The class configures a 3-second anonymous TTL and anAnonymousLinkRedirectsUntilItsExpiryPasses sleeps half of it, then requires a click to return 302. The guard `halfWay.isBefore(link.expiresAt())` is read before the request is sent, so a click that straddles the expiry passes the guard and fails on the 302 \u2014 and a served redirect makes up to three sequential Redis round trips each carrying the configured 1s command timeout, against ~1.4s of remaining margin. This is the one behaviour pinning \"live for the whole period it was promised\" (AC10); if it flakes it gets muted and takes that claim with it."
    },
    {
      "confidence": "high",
      "file": "service/src/test/java/com/example/urlshortener/unit/HostCanonicalisationTest.java",
      "id": "TEST-5",
      "line": 82,
      "severity": "nit",
      "suggestion": "Annotate the method @ResourceLock(Resources.LOCALE) so JUnit serialises it if parallel execution is ever enabled, and leave the assertion as it is.",
      "summary": "caseIsNotPartOfAHost mutates JVM-global state (Locale.setDefault(\"tr\")) to prove the lower-casing is locale-independent. It is restored in a finally and there is no junit-platform.properties enabling parallelism, so it is safe today \u2014 but it is a shared-JVM global in a suite that boots Spring contexts, and the day parallel execution is turned on this becomes order-dependent flakiness in whatever else happens to format a string. The behaviour is worth pinning; the hazard is worth declaring."
    },
    {
      "confidence": "medium",
      "file": "service/src/main/java/com/example/urlshortener/threat/DenylistThreatCheck.java",
      "id": "TEST-6",
      "line": 56,
      "severity": "nit",
      "suggestion": "If it is worth keeping, pin it where it is observable: a unit test calling DenylistThreatCheck.evaluate directly with URI.create(\"http://4294967296/\") and asserting ThreatVerdict.BLOCK, independent of the validator having run first.",
      "summary": "The fail-closed branch added here (canonical.isEmpty() -> BLOCK) is unreachable through any endpoint: UrlValidator.requireShortenable already throws url_rejected on an un-canonicalisable host before the threat check runs, so no test would notice if this branch were deleted. Its own comment states the point \u2014 \"a security property that holds only because they run in a particular order is one reordering away from not holding\" \u2014 and that property, independence from ordering, is precisely what no test asserts. Defence in depth deliberately added, recorded so it is a decision rather than dead code somebody later prunes."
    }
  ],
  "lens": "review-test-adequacy",
  "not_examined": [
    "The 132 pre-existing test methods: I read DegradedDependencyTest in full (to judge whether AnonymousCreateDegradationTest's 201-or-503 tolerance matched an existing convention) and grepped the rest. AC17's \"the existing test suite passes unchanged\" I took from the verify node's maven_verify gate, not by reading those tests.",
    "I did not run the suite. Every claim about flakiness, timing margin or a test passing after a hypothetical revert is from reading the code, not from execution or repeated runs.",
    "HostNormalizer's implementation body (147 lines of hand-rolled numeric parsing) was not audited against the unit expectations line by line. I checked that HostCanonicalisationTest pins the frozen javadoc table, not that the implementation satisfies it \u2014 the security lens owns whether the parsing is right.",
    "Whether the AC6 concurrency behaviour actually forces overlapping inserts on this hardware. It races six threads from a latch and asserts one row afterwards, but the interleaving cannot be forced; a single green run is weaker evidence than repeated runs, as test-contract.json itself notes.",
    "The V3/V4 migration SQL beyond reading their intent, and the duplicate-email precondition V3 carries \u2014 unfalsifiable from this repository, already recorded in docs/TODO.md.",
    "redis/*.lua and the rate-limiter internals; I judged the new buckets only from their HTTP behaviour in SignUpRateLimitTest and AnonymousCreateRateLimitTest.",
    "Whether the IP-keyed buckets are genuinely per-caller: every test in this suite originates from loopback, so \"one address throttled while another is served\" is untested for both new buckets. test-contract.json states this as a known limit of the harness rather than an omission.",
    "artifacts/review.json, release.json and the existing artifacts/review-test-adequacy.json describe the previous (greenfield) run; I read them only for context and did not re-audit their findings."
  ],
  "summary": "Unusually strong test work, and the headline failure mode this lens hunts is absent: no existing test file was touched (132 methods before, 242 after \u2014 the 110 new behaviours are all new methods in 15 new classes, each named for one behaviour with its own arrangement), so nothing is smuggled in as an appended assertion. Every AC1-AC17 maps to at least one test I read, and for each new behaviour I can name the test that goes red if it is reverted: AC1/AC2 at both levels (HostCanonicalisationTest pins the frozen normaliser's whole javadoc table including the rows that must NOT change \u2014 notmalware.example.com and malware.example.com.evil.test survive intact \u2014 while HostEvasionRefusalTest drives the same spellings over HTTP and pins the 400/422 split in both directions); AC6's race from a released CountDownLatch plus a storage check that a service returning one 201 while writing two rows would fail; AC7 against the frozen PasswordHasher and the stored form; AC13 structurally via customer_id IS NULL, so a placeholder-account implementation that answered 404 everywhere would still fail; AC11's counting read from the durable column rather than durable+pending, which closes the exact blind spot the previous run's review found. Assertions are specific (byte-identical body comparisons, exact property sets, inclusive boundaries exercised on both sides), fixture lists iterated are all non-empty, and I found no tautological or setup-guaranteed assertion. Six findings, no blockers. The one that matters is TEST-1: AC3 demands tests that fail against the pre-change code, and the pipeline ran author-tests concurrently with implement and first executed the suite at `verify`, after the fix \u2014 so the required evidence does not exist, and the green suite says nothing about it. The remaining five are small: one AC15 behaviour that tolerates success and therefore cannot fail, one genuinely unpinned new behaviour (the canonicalised own-host comparison, invisible because the test origin is localhost and already internal), one thin timing margin, and two nits."
}