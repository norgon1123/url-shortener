{
  "findings": [
    {
      "confidence": "medium",
      "file": "service/src/main/resources/db/migration/V3__unique_lower_email.sql",
      "id": "PERF-1",
      "line": 27,
      "severity": "major",
      "suggestion": "Make the constraint and the query agree: either add `CREATE INDEX ix_customers_upper_email ON customers (upper(email))`, or change the finder to an explicit JPQL `where lower(c.email) = lower(:email)` so the lower(email) index serves both roles. Confirm before shipping by turning on `spring.jpa.properties.hibernate.show_sql` in a test run to capture the generated predicate, then EXPLAIN it against a customers table seeded with ~100k rows and check for Index Scan rather than Seq Scan.",
      "summary": "The new unique index is on lower(email), but the only query that reads by email is CustomerRepository.findByEmailIgnoreCase, which Spring Data renders as `where upper(c.email) = upper(?)`. A functional index on lower(email) cannot serve an upper() predicate, and ux_customers_email (plain email) cannot serve it either, so sign-in seq-scans customers on every attempt. That was cheap while customers held two seeded rows; this change makes the table grow without bound from an unauthenticated endpoint, so sign-in latency now grows linearly with sign-up volume, and sign-in is already the slowest endpoint in the service (an Argon2 verify runs on every attempt, including unknown accounts). The index is correct as a constraint; it is just not the index the read path needs."
    },
    {
      "confidence": "medium",
      "file": "service/src/main/java/com/example/urlshortener/auth/SignUpService.java",
      "id": "PERF-2",
      "line": 60,
      "severity": "major",
      "suggestion": "Bound the hashing work rather than only metering the callers: put sign-up (and ideally sign-in) hashing behind a Semaphore or a fixed executor sized to the available cores, refusing with 429/503 past the queue rather than accumulating threads. Measure by running N concurrent sign-ups (N = 2x cores and 10x cores) while sampling redirect p99 in the same process; the number that matters is redirect latency under sign-up load, not sign-up throughput. Also record the JVM heap/container memory headroom against 200 x 16 MiB in the runbook.",
      "summary": "POST /api/v1/customers puts an Argon2id hash (m=16384 KiB, t=2, ~25 ms, 16 MiB transient per call) on an unauthenticated path with no global concurrency bound. Tomcat's default 200 request threads means a worst case of ~200 concurrent hashes, i.e. ~3.2 GiB of transient allocation and full CPU saturation in the same JVM that serves the redirect path, which has no CPU isolation from it. The only defence is the per-IP SIGN_UP bucket at 60/min, which (a) does not bound aggregate concurrency across many source addresses and (b) fails open by design when Redis is unreachable (RedisTokenBucketRateLimiter line 67-ish), so the vector is fully open in exactly the degraded state where the service is least able to absorb it. The duplicate-sign-up path pays the full hash before the constraint refuses it, so a repeat flood against one known address costs the same as real sign-ups. Sign-in already has this shape, so this is a second equal entry point rather than a new class of problem \u2014 but it doubles it, and it is the one being added."
    },
    {
      "confidence": "medium",
      "file": "service/src/main/java/com/example/urlshortener/link/LinkService.java",
      "id": "PERF-3",
      "line": 137,
      "severity": "major",
      "suggestion": "Take the cache invalidation off the open transaction \u2014 commit the insert, then invalidate before returning (the pre-issue ordering still holds relative to the response) \u2014 or at minimum set spring.datasource.hikari.maximum-pool-size explicitly against expected concurrency and document the interaction. Confirm by running the existing AnonymousCreateDegradationTest variant with Redis latency injected (e.g. a proxy adding 800 ms) while watching hikaricp_connections_pending and redirect p99 on /actuator/prometheus.",
      "summary": "createAnonymous holds an open database transaction across a Redis round trip: saveAndFlush inserts the row, then resolutionCache.invalidateBeforeIssuing does a network call to Redis with a 1s timeout, all inside @Transactional. The connection pool is Hikari at its default maximum of 10 (nothing in application.yml sizes it). When Redis is slow rather than down \u2014 the common failure \u2014 each create pins a pooled connection for up to ~1s in the evict alone, plus up to ~1s more in the rate-limit call before the handler is entered; ten concurrent creates exhaust the pool and the redirect path's cache-miss fallback to PostgreSQL then queues behind them. The failure is self-amplifying: a degraded Redis makes the limiter fail open, so the anonymous endpoint becomes unmetered at precisely the moment each request is holding a connection for a second. The shape is pre-existing in create(), but create() is authenticated and customer-bucketed; this replicates it on an unauthenticated, IP-bucketed path."
    },
    {
      "confidence": "high",
      "file": "service/src/main/java/com/example/urlshortener/threat/DenylistThreatCheck.java",
      "id": "PERF-4",
      "line": 76,
      "severity": "minor",
      "suggestion": "Replace the loop with a single `boolean existsByHostIn(Collection<String> candidates)` over the pre-computed parent chain (at most ~6 entries, bounded by label count), turning N round trips into one. Nothing about the label-based matching semantics changes.",
      "summary": "isListed issues one existsByHost query per label of the host: a.b.c.example.com costs five sequential database round trips, and each create pays this before it inserts. The rows are index-hits so each is cheap, but the round trips are serialised inside the create transaction and this change puts the same loop on an unauthenticated endpoint at 30/min/IP. Creates are not the hot path, so this is latency on a low-volume route rather than a capacity problem \u2014 filed because the fix is one line and the loop is now reachable without a credential."
    },
    {
      "confidence": "medium",
      "file": "service/src/main/resources/db/migration/V4__links_customer_id_nullable.sql",
      "id": "PERF-5",
      "line": 24,
      "severity": "minor",
      "suggestion": "Make it a partial index: `CREATE INDEX CONCURRENTLY ix_links_owner_listing_owned ON links (customer_id, created_at DESC, code ASC) WHERE customer_id IS NOT NULL`, then drop the old one, in a follow-up migration (CONCURRENTLY cannot run inside Flyway's transaction \u2014 needs the mixed/non-transactional setting). Worth doing only once anonymous rows are a measurable fraction of the table; check with `SELECT count(*) FILTER (WHERE customer_id IS NULL), count(*) FROM links` and pg_relation_size on the index.",
      "summary": "ix_links_owner_listing (customer_id, created_at DESC, code ASC) will now index anonymous rows it can never serve: every owner-scoped query is an equality match on customer_id and SQL equality never matches NULL, so each anonymous link adds an index entry that no query can read. On the busiest table in the system, at a permitted 30 creates/min/IP with no reaper, that is monotonic index growth for zero read benefit \u2014 larger index, slower vacuum, more buffer cache spent on unreachable tuples. Impact is proportional to anonymous adoption, which nobody can estimate yet; at low volume it is invisible."
    },
    {
      "confidence": "medium",
      "file": "service/src/main/resources/db/migration/V4__links_customer_id_nullable.sql",
      "id": "PERF-6",
      "line": 24,
      "severity": "minor",
      "suggestion": "Prepend `SET lock_timeout = '3s';` to V4 (and to V3) so the migration fails fast and retryably rather than queueing readers behind it, and note in the deploy runbook that the retry is expected behaviour. Confirm the lock class with `SELECT mode FROM pg_locks` during a rehearsal on a copy carrying production-sized links.",
      "summary": "ALTER TABLE links ALTER COLUMN customer_id DROP NOT NULL is a catalogue-only change and rewrites nothing, but it still needs an ACCESS EXCLUSIVE lock on links. Flyway runs it at application startup with no lock_timeout, so if any in-flight statement holds even a weak lock on links, the ALTER waits \u2014 and while it waits, every subsequent query on links queues behind it, including the redirect path's cache-miss fallback. The blast is bounded by the cache absorbing most reads for 60 seconds, but the mechanism turns a fast DDL into a stall on the hot table with no ceiling. V3's CREATE UNIQUE INDEX (not CONCURRENTLY) has the same shape against customers: it blocks writes on customers for the duration of the build, which is trivial today at two rows and stops being trivial once sign-up has been live a while."
    },
    {
      "confidence": "low",
      "file": "service/src/main/resources/db/migration/V4__links_customer_id_nullable.sql",
      "id": "PERF-7",
      "line": 18,
      "severity": "minor",
      "suggestion": "Ship a metric before the endpoint gets traffic: gauge `links` row count split by customer_id IS NULL, and total relation size, on the existing Prometheus endpoint. Decide a retention policy for expired anonymous rows (archive-then-delete keeping the code reserved) if the anonymous share passes some agreed fraction of the table.",
      "summary": "Anonymous links expire after 30 days but are never removed: V1 states 'nothing is ever deleted' and there is no reaper, so an anonymous row occupies the links table, the ux_links_domain_code index and the (domain, code) namespace permanently while being unredirectable after a month and undeletable by anyone (no owner, no admin endpoint). The migration comment names 'let the TTL drain' as the rollback plan, which is true of redirect behaviour but not of storage. I cannot size this from the code \u2014 it depends entirely on adoption of an endpoint that has never been exposed \u2014 so this is flagged as something to watch rather than something to fix now."
    }
  ],
  "lens": "review-performance",
  "not_examined": [
    "Nothing was run: no load test, no profiler, no query plans. Every claim about index usage is read from the generated-query shape and the migration text, not from EXPLAIN output.",
    "Hibernate's physical connection handling mode was assumed to be the Spring Boot default (delayed acquisition, release after transaction). If it acquires eagerly at transaction start, the Argon2 hash in SignUpService also runs while holding a pooled connection, which would upgrade PERF-2/PERF-3 into one worse finding.",
    "Lettuce/Spring Data Redis retry and connection-pool behaviour under partial failure was assumed from the 1s timeout in application.yml; I did not verify whether a call can exceed that (e.g. connect plus command, or reconnection attempts).",
    "The test sources under service/src/test were read only for what they imply about production behaviour; test runtime, container reuse and suite duration were not assessed (test-adequacy's lane).",
    "Actual Postgres lock classes for the two DDL statements were reasoned from the statement type, not observed on a real instance.",
    "JWT verification cost per authenticated request, the click flush job's behaviour at high link cardinality, and the abuse-report path were left alone as unchanged by this diff.",
    "No production or staging traffic figures were available, so every 'how often does this run' answer for the two new endpoints is a guess bounded by the configured rate limits (30/min and 60/min per source address) rather than a measurement."
  ],
  "summary": "The change does not touch the redirect hot path, and that is the most important thing to say: LinkResolver, ResolutionCache, RedirectController and the Redis click counter are byte-identical, the anonymous link resolves through the same (domain, code) unique index, and no per-click work is added. Nothing in this diff makes a redirect slower. The risk has moved onto the two new unauthenticated write endpoints and onto the schema. Three findings are worth a decision before ship: the new lower(email) unique index does not serve findByEmailIgnoreCase (which Spring Data renders with upper()), so sign-in seq-scans a table that this change makes grow without bound (PERF-1); sign-up puts a 16 MiB, ~25 ms memory-hard hash on an unauthenticated path with no global concurrency ceiling, defended only by a per-IP bucket that fails open when Redis is unreachable (PERF-2); and anonymous create holds a pooled connection (Hikari default 10, unsized) across a Redis call inside its transaction, which under a slow Redis is exactly when the limiter stops metering it (PERF-3). The rest is proportionate housekeeping: an N-round-trip denylist walk, an index that will carry rows it can never serve, two migrations without lock_timeout, and unbounded row growth I cannot size. At the traffic this service plausibly sees on creation endpoints \u2014 orders of magnitude below the click path \u2014 none of PERF-4 through PERF-7 will be felt for a long time; PERF-1 and PERF-2 are the ones whose cost lands on an existing endpoint (sign-in) and on shared CPU with the redirect path."
}